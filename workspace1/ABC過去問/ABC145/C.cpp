#include <bits/stdc++.h>
using namespace std;
int main(){
    int n;
    cin >> n;
    vector<double> x;
    vector<double> y;
    for(int i = 0; i < n; ++i){
        double a,b;
        cin >> a >> b;
        x.push_back(a);
        y.push_back(b);
    }

    for
}